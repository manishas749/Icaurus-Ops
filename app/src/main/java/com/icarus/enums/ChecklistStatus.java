package com.icarus.enums;

public enum  ChecklistStatus {
    SYNCHRONIZED, NOT_SYNCHRONIZED, RESOURCE_NOT_SYNCHRONIZED, EXECUTED_CHECKLIST

   /* SYNCHRONIZED(1), NOT_SYNCHRONIZED(3), RESOURCE_NOT_SYNCHRONIZED(2);

    private int checklistStatus;

    ChecklistStatus(int status) {
        this.checklistStatus = status;
    }

    public int getValue(){
        return this.checklistStatus;
    }*/
}
