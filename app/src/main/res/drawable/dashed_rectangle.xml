<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- we define background color -->
    <solid android:color="#fdfdfd" />
    <stroke
        android:width="2dp"
        android:color="@color/checklist_divider"
        android:dashWidth="6dp"
        android:dashGap="6dp" />



</shape>